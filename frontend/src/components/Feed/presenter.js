import React from "react";
import PropTypes from "prop-types";
import styles from "./styles.scss";

const Feed = (props, context) => <h3>Feed</h3>;

Feed.propTypes = {
  loading: PropTypes.bool.isRequired
};

export default Feed;
